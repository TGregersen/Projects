// Numbers that multiply to equal a third
// Created for a Code Jam.

int main(){
	cin>>T;
	for(int i=0; i<t; t++){
		vector <int> num;
		for(L){
			cin>>t;
			num.push_back(t);
		}
		sort(num.begin(),num.end());
		for(0->L){
			if(L-1 <= L/2){
				INDEX1=current;
				break;
			}
		}
		for(INDEX1->L){
			if(l-1 <= l/2){
				INDEX2=current;
				break;
			}
		}
		for(INDEX2<=L){ //n
			for(INDEX1<INDEX2){ //m
				if(mod(m,n)==0){
					p=mod(m,n);
				}
				for(0<INDEX1){ //o
					if(p==o){
						count++;
					}
				}
			}
		}
		cou<<"Case #"<<i<<": "<<count<<endl;
	}
}
